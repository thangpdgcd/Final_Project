import React from 'react';

/**
 * Placeholder widget shell.
 *
 * The customer-support widget logic lives under `src/features/customerSupportWidget/*`
 * and related hooks. This component is kept here so layouts can mount it safely.
 */
const CustomerSupportWidget: React.FC = () => {
  return null;
};

export default CustomerSupportWidget;

